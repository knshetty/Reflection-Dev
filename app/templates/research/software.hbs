{{#bs-nav type="pills" justified=true stacked=false as |nav|}}
  {{#each tabs as |tabItem|}}
    {{#nav.item}}
      {{#nav.link-to tabItem.linkTo}}
        {{tabItem.title}}
      {{/nav.link-to}}
    {{/nav.item}}
  {{/each}}
{{/bs-nav}}

{{outlet}}
